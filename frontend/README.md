Frontend (Vite + React)

Install & run

```bash
cd frontend
npm install
npm run dev
```

Notes
- Dev server proxies `/api` to `http://localhost:4000` (see `vite.config.js`).
- `VITE_API_BASE` can be used to override the API base URL.
